export enum AppConfigRootEnum {
	PORT = 'PORT',
	NODE_ENV = 'NODE_ENV',
	DATABASE_URL = 'DATABASE_URL',
	REDIS_URL = 'REDIS_URL',
}

export enum AppConfigGridEnum {
	SIZE = 'GRID.SIZE',
	MAX_CELL_CHANGES = 'GRID.MAX_CELL_CHANGES',
}
